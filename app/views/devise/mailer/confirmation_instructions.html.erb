<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account on FE', "#{ENV['HELPEXCHANGE_FRONTEND_ROOT_URL']}/users/login/#{@token}" %></p>
<p><%= link_to 'Confirm my account on BE', confirmation_url(@resource, confirmation_token: @token) %></p>
