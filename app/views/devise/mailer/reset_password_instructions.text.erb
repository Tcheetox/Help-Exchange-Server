Hello <%= @resource.first_name.blank? ? @resource.email : @resource.first_name %>,

Someone has requested to change your password. You can do this through the below link:
<%= "#{ENV['HELPEXCHANGE_FRONTEND_ROOT_URL']}/users/troubleshoot/reset/#{@token}" %>

Note that your password won't change until you access this link and create a new one.
If you are not the author of this request, please ignore this email.

Kind regards,
The FishForHelp team

        